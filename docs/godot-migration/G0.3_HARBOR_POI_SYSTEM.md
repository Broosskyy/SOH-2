# G0.3 Harbor + POI

**Harbor:** `HarborDefinition` + `HarborState` (OUTSIDE_PORT / PORT_APPROACH / IN_PORT) + `HarborEntity` approach/interaction radii.

**POI:** `PoiDefinition` types (HARBOR, LANDMARK, EVENT, …) with world labels via `WorldLabelService`.

G0.3 implements approach detection, interact enter/leave, and label projection. Economy services deferred.
