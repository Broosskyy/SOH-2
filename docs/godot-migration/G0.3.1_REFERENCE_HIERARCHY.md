# G0.3.1 Reference Hierarchy

## Binding rule

| Source | Role |
| --- | --- |
| **Group A** — current Godot hardware screenshots | Problem state only. Never a visual target. |
| **Group B** — V20.3.2 web build | Functional / gameplay / content migration source. |
| **Group C** — new Abyssal Dominion mockups | Binding visual / UX destination. |
| **Godot G0.3.1** | Production runtime combining B + C on preserved G0.3 architecture. |

## Architecture preserved (G0.3)

`GameUnit`, `ShipEntity`, `HealthComponent`, `TargetableComponent`, `TargetingSystem`, `RegionDefinition`, `RegionRuntime`, `IslandEntity`, harbor/POI/NPC domains, `NavigationController`, `WorldLabelService`, mobile web rendering fix, Kraken lock, camera lock.

## G0.3.1 presentation additions

- `IslandVisualBuilder` — readable temporary islands (no visible collision slabs)
- `HarborVisualBuilder` — pier / structures at harbors
- `NpcShipVisualBuilder` — sail-ship proxies with faction identity
- `GameplayHud` — top bar, mission panel, disabled combat stubs
- `Minimap` — 2D data-driven local chart
- `ocean_surface.gdshader` — lightweight tonal water motion
- `ShipWake` — quality-aware movement feedback
