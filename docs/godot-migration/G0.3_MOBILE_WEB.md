# G0.3 Mobile Web

**G0.2 fix retained:** `QualityManager.apply_directional_shadows()` syncs `DirectionalLight3D.shadow_enabled` with LOW profile atlas disable.

**G0.3 changes:**
- WebP island billboards removed (Android stretch artifact fix)
- Mobile Web viewport expand policy for landscape utilization
- Region/NPC/island systems validated under LOW emulation

**Hardware status:** READY FOR G0.3 RETEST — use HTTPS LAN server (`npm run serve:godot:web`).

Pre-G0.3 hardware: boots + renders after shadow fix; touch semantics not fully verified.
