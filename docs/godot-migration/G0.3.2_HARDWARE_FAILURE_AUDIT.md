# G0.3.2 Hardware Failure Audit

G0.3.1 real Android Mobile Web test: boot PASS, visual FAIL/PARTIAL.

## Observed failures (Group A)

| Issue | Root cause | G0.3.2 fix |
| --- | --- | --- |
| Radial island fan artifact | `_make_deformed_disc()` center-vertex fan + malformed side walls | `_make_solid_island_mesh()` ring top + quad walls + `generate_normals()` |
| HUD unreadably small | Fixed 8–13px fonts, no mobile scale factor | `HudLayout` 1.45–2.25× mobile scale on fonts, bars, panels |
| Minimap too small | Fixed 196×118 desktop size | Responsive 220×138+ mobile via `HudLayout` |
| Duplicate controls | `MobileControls` FEUER/1–3 + `GameplayHud` combat cluster | Mobile layer = KAMERA joystick + pinch only; combat hidden on mobile |
| Debug text on normal URL | Overlay shown on all debug Web builds | `MobileWebDiagnostics.query_flag("diag")` required on Web |
| Player floating HUD missing | Hidden in isolation step | Re-enabled `FloatingPlayerHUD` |
| World feels empty / islands oversized | `visual_scale = 0.88` | Reduced to `0.58`, preserved V20.3.2 catalog positions |
| Weak harbor identity | Minimal pier boxes | Coast berm, extended pier, warehouse, tower, beacon |

## Camera

**UNCHANGED** — production lock retained (FOV 35°, height 560, back 360, lateral −58, zoom 0.55–1.38). Mismatch was viewport/UI/world scale, not camera baseline.

## Player Kraken lock

**UNCHANGED** — scale 52, waterline 20.5, yaw 180°, HUD anchor 68.

## Status

**READY FOR REAL ANDROID G0.3.2 ACCEPTANCE** — hardware PASS not claimed from this pass.
