# G0.3.2 Reference Map

| Group | Files | Role |
| --- | --- | --- |
| **A** | `Gruppe_A_1`, `Gruppe_A_2` | Current Godot hardware state — problems to fix |
| **B** | `Gruppe_B_1`, `Gruppe_B_2` | Last V20.3.2 web build — functional migration source |
| **C** | `Gruppe_C_1` … `Gruppe_C_5` | Binding target mockups — visual destination |

## Binding rule

- **A → C** for presentation (camera, scale, HUD, composition, ocean identity)
- **B → Godot** for feature parity (mission text, currencies, nav labels, NPC groups, region layout)
- Do **not** make Godot look like B; use B for functionality only.

## G0.3.2 focus areas

1. Island mesh artifact recovery (`IslandVisualBuilder`)
2. Responsive HUD / minimap (`HudLayout`)
3. Single mobile control owner (`MobileControls` = camera only)
4. Diagnostic overlay gated on `?diag=1` on Web
5. World composition calibration (`visual_scale = 0.58`)
6. Harbor readability (`HarborVisualBuilder` coast + pier + tower)

Build marker: **G0.3.2** / `config/version=0.3.2`
