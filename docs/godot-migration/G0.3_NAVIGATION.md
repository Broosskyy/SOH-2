# G0.3 Navigation

Input path:

```
WorldPickInput / PlayerInputSource
    → NavigationController.command_to_destination()
    → PlayerShip.apply_command()
```

Island blocking uses `IslandEntity.blocks_point()` gameplay footprints, not visual mesh collision.

Tap water rejects destinations inside island boundaries. Desktop WASD bypasses navigation destination.
