# G0.3.1 Mobile Hardware

## Group A failure addressed

- Removed visible oversized cylinder island proxies from `VisualRoot`
- Restored V20.3.2 island positions via `_catalog(x,y)` transform
- Added presentation HUD + minimap with safe-area anchors

## Retest protocol

1. Fresh G0.3.1 web export (`build=g031` query param)
2. HTTPS LAN serve — dynamic IP, no hardcode
3. Landscape Android Chrome `?diag=1&build=g031`
4. Verify: no red/cyan slabs, readable islands, HUD + minimap, Kraken scale

## Status

**READY FOR G0.3.1 REAL ANDROID RETEST** — no hardware PASS claimed from this pass.

## Preserved fixes

- Directional shadow mobile web regression guard (`QualityManager.apply_directional_shadows`)
- Viewport expand policy for mobile web
- GL Compatibility renderer
