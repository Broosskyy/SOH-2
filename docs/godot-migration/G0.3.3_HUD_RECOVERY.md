# G0.3.3 HUD Recovery

## Floating player HUD

- Removed heavy `PanelContainer` black card
- Compact: name, LV, thin HP + shield bars
- `HudLayout.Semantic.FLOATING_PLAYER` — capped ~1.18× (not 2.25×)

## Top HUD

- Semantic `PLAYER_STATUS`, `MISSION`, `TARGET_STATUS`, `REGION`
- Mobile: menu button (disabled), nav row hidden
- Desktop: full nav row (mostly disabled except MISSION)

## Target panel

Center-top when `TargetingSystem` has ship target — name, level, HP bar.

## Combat presentation

- Desktop + mobile disabled clusters (FEUER + abilities)
- Single owner per cluster; mobile compact bottom-right

## Minimap

Circular compass frame, harbor dots, region name footer.

## Diagnostics

Overlay anchored bottom-left — does not cover mission panel.
