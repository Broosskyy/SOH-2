# Legacy G0.x Godot Runtime

The previous Web-first migration runtime (G0.5.x / G0.6 HUD V2) remains in Git history and under `godot/scripts/` legacy paths.

**M0 does not mount or execute these systems.**

Active entry: `scenes/boot/Boot.tscn` → `scenes/game/Game.tscn`

Recover old implementation from branch `main` or commits before `rebuild/native-foundation`.
